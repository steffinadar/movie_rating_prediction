# movie_rating_prediction
A Python machine learning project that predicts movie ratings using a RandomForestRegressor. It processes dataset features like genre, director, actors, runtime, and votes, applying one-hot encoding to handle categorical text data cleanly.
